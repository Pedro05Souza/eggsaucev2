from enum import Enum

class PlayerTitles(str, Enum):
    DEFAULT = "default"
    NOOB = "noob"
    BEGINNER = "beginner"
    INTERMEDIATE = "intermediate"
    ADVANCED = "advanced"
    EXPERT = "expert"
    MASTER = "master"
